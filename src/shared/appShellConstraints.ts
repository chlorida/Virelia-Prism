/** Minimum size for the main library/player shell (not mini player). */
export const APP_SHELL_MIN_WIDTH = 880;
export const APP_SHELL_MIN_HEIGHT = 640;

/** Viewport width at which the title-bar mode switch uses the full labeled rail. */
export const CONTENT_MODE_RAIL_MIN_WIDTH = 960;
